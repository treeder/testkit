{
  "name": "testkit",
  "version": "1.0.7",
  "description": "",
  "main": "testkit.js",
  "scripts": {
    "start": "npm install && npx wrangler dev --env dev --live-reload",
    "build": "npx wrangler pages functions build --outdir=./dist/",
    "start-server": "npm run build && npx wrangler dev --env dev",
    "test": "node test/test.js",
    "testkit": "node ./bin/testkit.js --port=8787"
  },
  "bin": "./bin/testkit.js",
  "keywords": [],
  "author": "treeder",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "api": "github:treeder/api",
    "dotenv": "^17.2.1"
  },
  "devDependencies": {
    "flaregun": "github:treeder/flaregun",
    "prettier": "^3.6.2"
  },
  "prettier": {
    "tabWidth": 2,
    "semi": false,
    "singleQuote": true,
    "trailingComma": "all",
    "printWidth": 120
  }
}
